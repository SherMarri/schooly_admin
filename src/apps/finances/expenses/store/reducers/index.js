const initialState = {
};

const expenses = function (state = initialState, action) {
    switch ( action.type )
    {
        // case Actions.SET_SEARCHBAR_VISIBILITY:
        // {
        //     return {
        //         ...state,
        //         searchbar_visible: action.payload
        //     };
        // }
        default:
        {
            return state
        }
    }
};

export default expenses;